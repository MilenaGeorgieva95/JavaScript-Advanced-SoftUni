function stringLength(firstStr, secStr, thirdStr) {
  const sum = firstStr.length + secStr.length + thirdStr.length;
  const average = Math.floor(sum / 3);
  console.log(sum);
  console.log(average);
}
